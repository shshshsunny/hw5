import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// https://vitejs.dev/config/
export default defineConfig({
  base: '/hw5/', // 確保子路徑正確
  plugins: [vue()],
  build: {
    outDir: 'dist', // 🛠️ 強制指定打包出來的資料夾名稱叫做 dist
  }
})
